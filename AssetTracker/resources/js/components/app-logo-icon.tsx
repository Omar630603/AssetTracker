// app-logo-icon.tsx
import { Radio } from 'lucide-react';
import { ComponentProps } from 'react';

export default function AppLogoIcon(props: ComponentProps<typeof Radio>) {
    return <Radio {...props} />;
}
